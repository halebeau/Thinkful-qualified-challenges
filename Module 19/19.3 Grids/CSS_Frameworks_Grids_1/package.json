{
  "name": "zid-fe-css-frameworks-02-grids",
  "version": "1.0.0",
  "description": "CSS Frameworks: Grids",
  "main": "src/server.js",
  "scripts": {
    "docker:build": "docker image build . -t thinkful-ed/zid-fe-css-frameworks-02-grids",
    "docker:run": "docker run --rm -it -p 3000:3000 thinkful-ed/zid-fe-css-frameworks-02-grids",
    "docker:stop": "docker stop $(docker ps -q)",
    "docker:test": "docker run -t thinkful-ed/zid-fe-css-frameworks-02-grids npm test",
    "start:solution": "npm run -it docker:build && npm run docker:run",
    "test": "jest",
    "test:solution": "npm run docker:build && npm run docker:test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "jest": "^26.4.2"
  }
}
